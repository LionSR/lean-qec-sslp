# Shell scripts must keep LF endings even when checked out on Windows: git's
# core.autocrlf=true would otherwise rewrite them to CRLF and bash then fails with
# "$'\r': command not found" on the first line of the script body.
*.sh text eol=lf
